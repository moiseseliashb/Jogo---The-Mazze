class LightingRenderer:
    """ def __init__(self, screen):
        self.screen = screen
        self.darkness = pygame.Surface(screen.get_size(), pygame.SRCALPHA)
        self.light_gradient = pygame.Surface(screen.get_size(), pygame.SRCALPHA)
        self.light_mask = pygame.Surface(screen.get_size(), pygame.SRCALPHA) """
    pass